{
  "name": "ai-blog-generator-v2",
  "version": "2.0.0",
  "type": "module",
  "description": "AI-powered blog content generator with complete blog management system - Version 2.0",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "./deploy.sh",
    "deploy:manual": "npm run build && wrangler pages deploy dist --project-name ai-blog-generator-v2",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name ai-blog-generator-v2",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000",
    "git:commit": "git add . && git commit -m"
  },
  "dependencies": {
    "hono": "^4.9.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@cloudflare/workers-types": "4.20250705.0",
    "typescript": "^5.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}